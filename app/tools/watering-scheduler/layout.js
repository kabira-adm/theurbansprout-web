export const metadata = {
  title: "Watering Scheduler",
  description:
    "A quick calculator that suggests a watering frequency for your plant based on type, pot size, season, and location — a starting point, not a substitute for checking the soil.",
};

export default function WateringSchedulerLayout({ children }) {
  return children;
}
